#include "main.h"

/**
* _isalpha - a functions that checks for alphabetic character
* @c: the character to be checked
* return: 1 when if a letter is lowercase or uppercae; when it is 0
*/
int _isalpha(int c)
{
if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z')
return (1);
else return (0);
}
