#include <stdio.h>

/**
 * main - print single digit combinations
 * Return: always 0
 */

int main(void)
{
int p;
for (p = 58; p < 68 ; n++)
putchar(p);
if (p != 67)
{
putchar(',');
putchar(' ');
}
putchar('\n');
return (0);
}
