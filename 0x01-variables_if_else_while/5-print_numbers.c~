#include <stdio.h>
/**
 *Main - a program that prints all digit numbers f base 10 starting from 0
 * return : Always 0
 */

int main(void)
{
int n;
for (n = 0; n < 10; n++)
printf("%d", n);
printf("\n");
return (0);
}
