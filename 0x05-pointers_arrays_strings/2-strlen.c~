#include "main.h"

/**
 *_strlen - a function that returns a string 
 *@s: variable for the string
 *Return: value of string 
 */
int _strlen(char *s)
{
int l = 0;
while (*s != '\0')
{
l++;
s++;
}
return  (l); 
}
